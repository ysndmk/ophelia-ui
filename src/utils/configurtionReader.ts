function configurtionReader(config: { apiUrl: any; apiKey: any; }) {
    // Use the provided configuration here
    console.log('API URL:', config.apiUrl);
    console.log('API Key:', config.apiKey);
  }
  
  export default configurtionReader;